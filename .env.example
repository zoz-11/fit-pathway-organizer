# Copy to .env and fill with your project values.
# WARNING: never commit real secrets to the repository.

# Vite public supabase keys (start with VITE_ so Vite exposes them to the client bundle)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-public-anon-key

# For server-side secrets (do NOT expose via VITE_), use SERVER_ or other names and keep them out of client bundles.
# Example:
# SERVER_SUPABASE_SERVICE_KEY=your-service-role-key

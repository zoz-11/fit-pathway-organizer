#!/bin/bash
# If this is your first push to this repository, use:
git push -u origin main

# If your default branch is named 'master' instead of 'main', use:
# git push -u origin master

# If you've already set up tracking and just want to push new commits:
# git push